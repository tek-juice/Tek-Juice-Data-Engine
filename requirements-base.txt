# ─────────────────────────────────────────────────────────────────────────────
# DATA ENGINE — Heavy / Compiled Base Dependencies
# Installed once into Dockerfile.base → shared by all service images.
# Only put packages here that: (a) take a long time to compile/download, OR
# (b) are shared across every service image.
# ─────────────────────────────────────────────────────────────────────────────

# ── Large ML/NLP stack (torch is ~350 MB, scipy ~106 MB, etc.)
torch==2.3.1
numpy==1.26.4
scipy==1.13.1
scikit-learn==1.5.0
faiss-cpu==1.8.0
transformers==4.41.2
sentence-transformers==3.0.1
spacy==3.7.5
spacy-transformers==1.3.8
tiktoken==0.7.0
nltk==3.8.1

# ── Compiles C extensions at install time
lxml==5.2.2
aiohttp==3.9.5
psycopg2-binary==2.9.9
asyncpg==0.29.0
hiredis==2.3.2
cryptography==42.0.8
Pillow==10.3.0
orjson==3.10.5
ujson==5.10.0

# ── Playwright (large binary download — separate step below is better,
#    but keeping here so it's only fetched once)
playwright==1.44.0
