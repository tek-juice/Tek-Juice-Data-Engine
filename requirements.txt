# ─────────────────────────────────────────────────────────────────────────────
# DATA ENGINE — Runtime Service Dependencies
# Installed on top of the shared base image (Dockerfile.base).
# Keep this list small — heavy compiled packages live in requirements-base.txt,
# dev/test tools live in requirements-dev.txt.
# ─────────────────────────────────────────────────────────────────────────────

# ── Web Framework
fastapi==0.111.0
uvicorn[standard]==0.30.1
httpx==0.27.0

# ── Database
sqlalchemy==2.0.31
alembic==1.13.1
pgvector==0.3.2

# ── Redis / Caching
redis==5.0.7

# ── Task Queue
celery==5.4.0
flower==2.0.1

# ── Data Validation
pydantic==2.7.4
pydantic-settings==2.3.4
email-validator==2.2.0

# ── Authentication & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.9

# ── Embedding Providers (API clients only — no heavy deps)
openai==1.35.7
google-generativeai==0.7.2
voyageai==0.2.3

# ── Web Scraping
beautifulsoup4==4.12.3
requests==2.32.3

# ── Schema / Structured Data
jsonschema==4.22.0
rdflib==7.0.0

# ── Monitoring & Observability
prometheus-client==0.20.0
opentelemetry-api==1.25.0
opentelemetry-sdk==1.25.0
opentelemetry-instrumentation-fastapi==0.46b0
structlog==24.2.0

# ── Background / Scheduling
apscheduler==3.10.4
croniter==2.0.5

# ── File Handling
python-magic==0.4.27
PyPDF2==3.0.1
python-docx==1.1.2
openpyxl==3.1.4

# ── Utilities
python-dotenv==1.0.1
tenacity==8.3.0
arrow==1.3.0
shortuuid==1.0.13
rich==13.7.1
